{
  "name": "html-editor-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "HTML editor with AI rewrite proxy server",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "stop": "node scripts/stop-server.js",
    "restart": "npm run stop && npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
